<UserControl x:Class="TestXMLModern.MVVM.View.TimerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TestXMLModern.MVVM.View"
             mc:Ignorable="d" 
             d:DesignHeight="480" d:DesignWidth="700">
    <UserControl.Resources>
        <!-- Стиль для кнопок управления -->
        <Style x:Key="ControlButton" TargetType="Button">
            <Setter Property="Background" Value="#373547"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Width" Value="120"/>
            <Setter Property="Height" Value="45"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="15">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#4A485F"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Стиль для слайдера -->
        <Style x:Key="TimerSlider" TargetType="{x:Type Slider}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Slider}">
                        <Grid>
                            <Border x:Name="Track" Height="6" Background="#2E2C3A" CornerRadius="3"/>
                            <Track x:Name="PART_Track">
                                <Track.Thumb>
                                    <Thumb x:Name="Thumb" Focusable="False" Height="22" Width="22">
                                        <Thumb.Template>
                                            <ControlTemplate TargetType="{x:Type Thumb}">
                                                <Grid>
                                                    <Ellipse Fill="#784ff2" Stroke="White" StrokeThickness="2"/>
                                                </Grid>
                                            </ControlTemplate>
                                        </Thumb.Template>
                                    </Thumb>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Верхняя панель с таймером и слайдером -->
        <StackPanel Grid.Row="0" HorizontalAlignment="Center" VerticalAlignment="Center">
            <!-- Дисплей таймера -->
            <TextBlock Text="{Binding TimeDisplay}"
                       Foreground="White"
                       FontSize="120"
                       FontWeight="Light"
                       HorizontalAlignment="Center"/>

            <!-- Слайдер для выбора времени -->
            <Slider Value="{Binding SelectedMinutes, Mode=TwoWay}"
                    Minimum="1"
                    Maximum="180"
                    Width="350"
                    Margin="0,20,0,0"
                    Style="{StaticResource TimerSlider}"/>

            <TextBlock Text="{Binding SelectedMinutes, StringFormat={}{0:F0} мин.}"
                       Foreground="#888"
                       FontSize="14"
                       HorizontalAlignment="Center"
                       Margin="0,5,0,0"/>

            <!-- Кнопки -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,30,0,0">
                <Button Content="Старт" Command="{Binding StartCommand}" Style="{StaticResource ControlButton}" Margin="10"/>
                <Button Content="Стоп" Command="{Binding StopCommand}" Style="{StaticResource ControlButton}" Margin="10"/>
                <Button Content="Сброс" Command="{Binding ResetCommand}" Style="{StaticResource ControlButton}" Margin="10"/>
            </StackPanel>
        </StackPanel>

        <!-- Нижняя панель с историей -->
        <Border Grid.Row="1" Background="#2E2C3A" CornerRadius="15,15,0,0" Margin="20,0,20,0" MaxHeight="150">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Text="История сессий" Foreground="White" FontWeight="Bold" Margin="15"/>
                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="15,0,15,15">
                    <ItemsControl ItemsSource="{Binding SessionHistory}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="0,2">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="{Binding Date, StringFormat='dd.MM.yyyy HH:mm'}" Foreground="#AAA"/>
                                    <TextBlock Grid.Column="1" Text="{Binding DurationMinutes, StringFormat={}{0} мин.}" Foreground="White" FontWeight="SemiBold"/>
                                </Grid>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Grid>
        </Border>
    </Grid>
</UserControl>
