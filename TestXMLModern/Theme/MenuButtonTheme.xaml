<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Style x:Key="MenuButtonTheme" TargetType="RadioButton">
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="RadioButton">
                    <Border Background="{TemplateBinding Background}"
                            BorderThickness="0"
                            CornerRadius="10"
                            Margin="15,0,15,5">
                        <Grid>
                            <!-- Индикатор выбранной кнопки -->
                            <Border x:Name="Indicator"
                                    Background="#784ff2"
                                    HorizontalAlignment="Left"
                                    Width="4"
                                    CornerRadius="2"
                                    Visibility="Collapsed"/>

                            <!-- Текст кнопки -->
                            <TextBlock Text="{TemplateBinding Content}"
                                       Foreground="{TemplateBinding Foreground}"
                                       VerticalAlignment="Center"
                                       HorizontalAlignment="Left"
                                       FontSize="14"
                                       Margin="30,0,0,0"/>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <!-- Когда кнопка выбрана (IsChecked = True) -->
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="Indicator" Property="Visibility" Value="Visible"/>
                            <Setter Property="Background" Value="#2E2C3A"/>
                        </Trigger>
                        <!-- Когда на кнопку навели мышь -->
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#3A384A"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
